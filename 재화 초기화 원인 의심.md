  확정된 메커니즘

  ResetToDefault()와
  InitializeCurrencies()는 기존
  유저 세션 중에 호출되지
  않는다.
  InitializePlayerData()는
  !isNewAccount 가드로 차단,
  ResetToDefault()는 치트
  커맨드와 신규 유저 경로에만
  존재한다.

  즉 세션 중 누군가가 재화를
  0으로 만든 것이 아니라,
  처음부터 메모리에 0으로 올라온
   것이다.

  ---
  실제 원인 경로

  08:30:21 loadAll 응답
      └─ additionalCurrencies
  리스트가 불완전하게 파싱됨
          └─ ApplyAdditionalCurr
  enciesFromSaveData에서
             ac_5, ac_6, ac_7...
   → 메모리에 0으로 남음
             ac_220 = 1 → 정상
  적용 (리스트에 존재했거나
  named field)
             ac_1(gold),
  ac_2(gem), ac_3(stamina),
  ac_10, ac_12 → named field로
  정상 적용

  (47분간 세션 진행)
      └─ 수정된 재화: ac_1 +151,
   ac_2 -915, ac_3 +4, ac_10
  -1767, ac_12 -46, ac_220 +2
      └─ 미수정 재화: ac_5,
  ac_6, ac_7... → 계속 메모리에
  0

  09:17:43 ModifyCurrency 호출
  (로그인 보상 등)
      └─ autoSave = true →
  SaveCurrencyDataAsync() 트리거
      └─ BuildAdditionalCurrenci
  esForSave(): amount <= 0 skip
          → ac_5, ac_6, ac_7...
  미포함
      └─ 서버에 전송:
  {gold:3300, gem:100, additiona
  lCurrencies:[ac_220:3, ...]}
      └─ 서버가 currency 문서
  전체 교체
      └─ DB: ac_5=3885 → 0 (해당
   키 삭제)

  PlayerData의
  cumulativeLoginDays=0,
  lastSaveTime=0도 동일한
  패턴이다. level, exp,
  totalGamePlayCount는 정상인데
  cumulativeLoginDays와
  lastSaveTime만 0인 것은 08:30
  loadAll 응답에서 PlayerData
  일부 필드도 정상 파싱되지
  않았음을 보여준다. 재화와
  플레이어 데이터 모두 같은
  세션에서 같은 문제가 발생했다.

  ---
  원인 특정을 위해 확인할 로그

  1순위 — 08:30:21 loadAll
  response body

  이것 하나로 원인이 확정된다.

  확인 포인트:
  -
  currency.additionalCurrencies
  배열의 항목 수와 내용
    → ac_220 이 포함됐는지,
  ac_5(=ECurrencyType 5) 가
  있었는지
  - player.cumulativeLoginDays
  값
    → 서버가 0을 반환했는지,
  아니면 응답에 없었는지

  서버가 정상값을 반환했는데
  클라이언트가 0으로 적용했다면
  → 클라이언트 파싱 버그
  서버가 처음부터 0이나 빈
  배열을 반환했다면 → 서버 응답
  문제

  2순위 — 09:17:43 saveAll
  request body

  확인 포인트:
  - allData 배열에서 currency
  항목의 additionalCurrencies
  내용
    → ac_5, ac_6 등이
  포함됐는지, 아니면 완전히
  없는지
  - currency 외 다른 dataType도
  함께 전송됐는지

  3순위 — Crashlytics 클라이언트
   로그 (user 1RUY6Q,
  08:30~09:17 구간)

  검색 키워드:
  - "[CurrencyResetGuard]"
  - "[PlayerDataManager]
  Currency 데이터 로드"
  - "additionalCurrencies"
  - "ApplyAdditionalCurrenciesFr
  omSaveData"
  - "ECurrencyType 정의되지
  않음" 또는 enum 관련 skip 로그

  특히 ApplyAdditionalCurrencies
  FromSaveData에서 enum 미정의로
   skip된 항목이 있으면 로그에
  남았을 가능성이 있다. 없다면
  로깅 추가가 필요하다.

  4순위 — 해당 유저의 이전 세션
  currency save 기록

  09:17:43 이전의 마지막 saveAll
   시점 확인
  → 그 저장에서
  additionalCurrencies에 ac_5,
  ac_6 등이 있었는지
  → 없었다면 언제부터 사라졌는지

  이 네 가지 중 1순위(08:30
  loadAll response body)가 가장
  직접적인 답을 준다.

✻ Crunched for 6m 44s

────────────────────────────────
❯  
────────────────────────────────
  ? for shortcuts
  정상 적용

  (47분간 세션 진행)
      └─ 수정된 재화: ac_1 +151, ac_2 -915,
  ac_3 +4, ac_10 -1767, ac_12 -46, ac_220 +2
      └─ 미수정 재화: ac_5, ac_6, ac_7... →
  계속 메모리에 0

  09:17:43 ModifyCurrency 호출 (로그인 보상
  등)
      └─ autoSave = true →
  SaveCurrencyDataAsync() 트리거
      └─ BuildAdditionalCurrenciesForSave():
  amount <= 0 skip
          → ac_5, ac_6, ac_7... 미포함
      └─ 서버에 전송: {gold:3300, gem:100,
  additionalCurrencies:[ac_220:3, ...]}
      └─ 서버가 currency 문서 전체 교체
      └─ DB: ac_5=3885 → 0 (해당 키 삭제)

  PlayerData의 cumulativeLoginDays=0,
  lastSaveTime=0도 동일한 패턴이다. level,
  exp, totalGamePlayCount는 정상인데
  cumulativeLoginDays와 lastSaveTime만 0인
  것은 08:30 loadAll 응답에서 PlayerData 일부
  필드도 정상 파싱되지 않았음을 보여준다.
  재화와 플레이어 데이터 모두 같은 세션에서
  같은 문제가 발생했다.

  ---
  원인 특정을 위해 확인할 로그

  1순위 — 08:30:21 loadAll response body

  이것 하나로 원인이 확정된다.

  확인 포인트:
  - currency.additionalCurrencies 배열의 항목
  수와 내용
    → ac_220 이 포함됐는지,
  ac_5(=ECurrencyType 5) 가 있었는지
  - player.cumulativeLoginDays 값
    → 서버가 0을 반환했는지, 아니면 응답에
  없었는지

  서버가 정상값을 반환했는데 클라이언트가
  0으로 적용했다면 → 클라이언트 파싱 버그
  서버가 처음부터 0이나 빈 배열을 반환했다면 →
   서버 응답 문제

  2순위 — 09:17:43 saveAll request body

  확인 포인트:
  - allData 배열에서 currency 항목의
  additionalCurrencies 내용
    → ac_5, ac_6 등이 포함됐는지, 아니면
  완전히 없는지
  - currency 외 다른 dataType도 함께
  전송됐는지

  3순위 — Crashlytics 클라이언트 로그 (user
  1RUY6Q, 08:30~09:17 구간)

  검색 키워드:
  - "[CurrencyResetGuard]"
  - "[PlayerDataManager] Currency 데이터 로드"
  - "additionalCurrencies"
  - "ApplyAdditionalCurrenciesFromSaveData"
  - "ECurrencyType 정의되지 않음" 또는 enum
  관련 skip 로그

  특히
  ApplyAdditionalCurrenciesFromSaveData에서
  enum 미정의로 skip된 항목이 있으면 로그에
  남았을 가능성이 있다. 없다면 로깅 추가가
  필요하다.

  4순위 — 해당 유저의 이전 세션 currency save
  기록

  09:17:43 이전의 마지막 saveAll 시점 확인
  → 그 저장에서 additionalCurrencies에 ac_5,
  ac_6 등이 있었는지
  → 없었다면 언제부터 사라졌는지

  이 네 가지 중 1순위(08:30 loadAll response
  body)가 가장 직접적인 답을 준다.